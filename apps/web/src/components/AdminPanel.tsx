import { type FormEvent, useEffect, useState } from 'react';
import { ArrowLeft, Check, FileText, Globe2, LockKeyhole, PackagePlus, UploadCloud } from 'lucide-react';
import { api, type Product } from '../lib/api';

export function AdminPanel({ onBack, onChanged }: { onBack: () => void; onChanged?: () => Promise<void> | void }) {
  const [products, setProducts] = useState<Product[]>([]); const [title, setTitle] = useState(''); const [slug, setSlug] = useState(''); const [description, setDescription] = useState(''); const [price, setPrice] = useState('199'); const [published, setPublished] = useState(true); const [status, setStatus] = useState(''); const [busyId, setBusyId] = useState('');
  async function refresh() { setProducts(await api.adminProducts()); }
  useEffect(() => { refresh().catch((error) => setStatus(error.message)); }, []);
  function makeSlug(value: string) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, ''); }
  async function createProduct(event: FormEvent) { event.preventDefault(); setStatus('Creating product…'); try { await api.adminCreateProduct({ title: title.trim(), slug: makeSlug(slug || title), description: description.trim(), pricePaise: Math.round(Number(price) * 100), published }); setTitle(''); setSlug(''); setDescription(''); setPrice('199'); setPublished(true); setStatus('Product created and ready for your PDF.'); await refresh(); await onChanged?.(); } catch (error) { setStatus(error instanceof Error ? error.message : 'Unable to create product.'); } }
  async function togglePublish(product: Product) { setBusyId(product.id); setStatus(product.published ? 'Taking product offline…' : 'Publishing product…'); try { await api.adminUpdateProduct(product.id, { published: !product.published }); setStatus(product.published ? 'Product unpublished.' : 'Product is now visible in the store.'); await refresh(); await onChanged?.(); } catch (error) { setStatus(error instanceof Error ? error.message : 'Could not change publishing status.'); } finally { setBusyId(''); } }
  async function upload(productId: string, file?: File) { if (!file) return; setBusyId(productId); setStatus('Uploading PDF securely…'); try { await api.adminUploadPdf(productId, file); setStatus('PDF uploaded securely.'); await refresh(); } catch (error) { setStatus(error instanceof Error ? error.message : 'Upload failed.'); } finally { setBusyId(''); } }
  return <section className="admin-page"><div className="admin-header"><div><button className="admin-back" onClick={() => { onBack(); window.setTimeout(() => window.location.reload(), 80); }}><ArrowLeft size={16} /> Store</button><div className="section-kicker">SKILLORA CONTROL ROOM</div><h1>Build your digital library.</h1><p>Create resources, attach protected PDFs and decide exactly when customers can see them.</p></div><div className="admin-stat"><PackagePlus size={20} /><strong>{products.length}</strong><span>resources</span></div></div><div className="admin-layout"><form className="admin-form" onSubmit={createProduct}><div className="admin-form-heading"><div className="admin-icon"><PackagePlus size={19} /></div><div><span>NEW RESOURCE</span><h2>Create product</h2></div></div><label>Title<input value={title} onChange={(e) => { setTitle(e.target.value); if (!slug) setSlug(makeSlug(e.target.value)); }} placeholder="Java Interview Guide" required minLength={3} /></label><label>Slug<input value={slug} onChange={(e) => setSlug(makeSlug(e.target.value))} placeholder="java-interview-guide" required /></label><label>Description<textarea value={description} onChange={(e) => setDescription(e.target.value)} placeholder="What will the customer learn or get from this resource?" minLength={20} required /></label><label>Price <span className="field-hint">INR</span><div className="price-input"><span>₹</span><input type="number" min="1" step="1" value={price} onChange={(e) => setPrice(e.target.value)} required /></div></label><label className="publish-switch"><input type="checkbox" checked={published} onChange={(e) => setPublished(e.target.checked)} /><span className="switch-track"><span /></span><span><strong>Publish immediately</strong><small>Show this product in the customer store after creation.</small></span></label><button className="primary-button large"><PackagePlus size={17} /> Create resource</button>{status && <div className="admin-notice">{status}</div>}</form><div className="admin-products"><div className="admin-list-heading"><div><span className="section-kicker">CATALOG</span><h2>Your resources</h2></div><span>{products.length} total</span></div>{products.length ? products.map((product) => <article className="admin-product" key={product.id}><div className="resource-thumb"><FileText size={24} /><span>PDF</span></div><div className="resource-main"><div className="resource-title"><h3>{product.title}</h3><span className={product.published ? 'status-live' : 'status-draft'}>{product.published ? <><Globe2 size={12} /> Live</> : <><LockKeyhole size={12} /> Draft</>}</span></div><p>₹{(product.pricePaise / 100).toLocaleString('en-IN')} · {product.slug}</p><small>{product.fileKey ? 'PDF attached and protected' : 'PDF not attached yet'}</small></div><div className="resource-actions"><button type="button" className={`status-button ${product.published ? 'live' : ''}`} disabled={busyId === product.id} onClick={() => togglePublish(product)}>{product.published ? <><Check size={15} /> Published</> : <><Globe2 size={15} /> Publish</>}</button><label className="upload-button"><UploadCloud size={15} /> {product.fileKey ? 'Replace PDF' : 'Upload PDF'}<input type="file" accept="application/pdf" onChange={(e) => upload(product.id, e.target.files?.[0])} /></label></div></article>) : <div className="admin-empty"><PackagePlus size={26} /><h3>Your catalog is empty.</h3><p>Create your first digital resource on the left.</p></div>}</div></div></section>;
}
